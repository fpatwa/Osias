os: linux
dist: bionic
arch: amd64
language: python
python:
    - "3.6"
before_install:
    - sudo apt-get update
install:
    - id
    - groups
    - jq
script:
    - source all_in_one_maas_vm_deployment.sh
