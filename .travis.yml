os: linux
dist: bionic
arch: amd64
before_install:
    - sudo apt-get update
    - sudo apt-get install -y ssh qemu-system-x86
language: python
python:
    - "3.6"
install:
    - pip install transient
script:
    - transient run -ssh-console -image centos/7:2004.01 -- -enable-kvm -m 1G <<< "uname -a"
